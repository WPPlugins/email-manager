<?php if ( 'wpem_section-mail' == $section['id'] ) : ?>

	<p><strong>Settings for mail.</strong></p>

<?php elseif ( 'wpem_section-notices' == $section['id'] ) : ?>

	<p><strong>Notices settings.</strong></p>

<?php elseif ( 'wpem_section-user' == $section['id'] ) : ?>

	<p><strong>Default user settings.</strong></p>
	
<?php elseif ( 'wpem_section-header' == $section['id'] ) : ?>

	<p><strong>Header settings.</strong></p>

<?php endif; ?>
